x=int(input("Enter the lower limit:"))
y=int(input("Enter the upper limit:"))
for i in range(x,y+1):
    if(i%3==0 and i%5==0):
        print("Divisible by by both 3 and 5:",i)
    elif(i%3==0):
        print("Divisible by 3 only:",i)
    elif(i%5==0):
        print("Divisible by 5 only:",i)
